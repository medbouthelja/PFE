doctrine:
    dbal:
        url: 'sqlite:///%kernel.project_dir%/var/data.db'
    orm:
        auto_mapping: true
        mappings:
            App:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Entity'
                prefix: 'App\Entity'
                alias: App
